#!/usr/bin/env -S nix-shell
--[[
#!nix-shell -i luajit -p luajit
--]]

local CACHE_PATH = "/tmp/ljcache/"
if os.rename(CACHE_PATH, CACHE_PATH) then
    os.execute("mkdir -p " .. CACHE_PATH)
end

-- vim: filetype=lua
